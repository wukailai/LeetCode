服务器端一般设置不主动关闭连接。但HTTP请求中，http头部connection参数可能设置为close，则服务端处理完请求会主动关闭TCP连接；
connection值为close，服务器端主动关闭TCP连接，服务器端有可能大量处于TIME_WAIT状态，进而造成端口被大量占用；
主动发起结束TCP连接的一端会处于TIME_WAIT状态（目的是解决ACK丢失重发，接收端收不到ACK会重发FIN），时间可设置，一般是2MLS；
滑动窗口（会动态调整）作用：流量控制，避免网络拥塞；