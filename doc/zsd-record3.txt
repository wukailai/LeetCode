分库分表中，数据库主键使用分布式的高并发唯一id生成器来生成；
mysql优化方式之一：使用先使用范围查询定位id（或者索引），然后再使用索引进行定位数据，能够提高好几倍查询速度。即先 select id，然后再 select *；
服务器优化方向：
    a）增加负载均衡服务器（让扩容也变得简单起来）；
    b）缓存静态文件（不长变化的文件，如图片、html文件）；
    c）缓存放在距离用户更近、性能更好的服务器（兼顾速度和运行负载）；
    d）压缩数据；
    e）实时监控及时发现问题；